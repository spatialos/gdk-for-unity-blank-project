package blank_project;

type HealRequest {
    int32 heal_amount = 1;
}

type HealResponse {}

enum HealType {
    PARTIAL = 0;
    FULL = 1;
}

type HealInfo {
    HealType heal_type = 1;
}

component Health
{
    id = 13131;

    int32 health = 1;

    command HealResponse heal(HealRequest);

    event HealInfo healed;
}
